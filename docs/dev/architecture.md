# Architecture

TaskSmack's canonical architecture document is:

**[Read `tasksmack.md` on GitHub](https://github.com/mgradwohl/tasksmack/blob/main/tasksmack.md)**

It defines the Platform, Domain, Core, UI, and App responsibilities; dependency rules; sampling threads; capability model; platform backends; configuration; and current engineering direction.

The docs site links to that source rather than maintaining a second dependency diagram that can drift from the code.
