name: 'Setup Windows LLVM'
description: 'Install LLVM 22, Ninja, and ccache on Windows via Chocolatey and add to PATH'

runs:
  using: 'composite'
  steps:
    - name: Install LLVM / Ninja / ccache
      shell: pwsh
      run: |
        choco install llvm --version 22.1.0 -y
        choco install ninja ccache -y
        # Refresh PATH so subsequent steps find clang, ninja, ccache without a new shell
        $env:PATH = "C:\Program Files\LLVM\bin;$env:PATH"
        echo "C:\Program Files\LLVM\bin" | Out-File -FilePath $env:GITHUB_PATH -Encoding utf8 -Append
