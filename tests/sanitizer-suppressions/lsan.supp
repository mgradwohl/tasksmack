# LeakSanitizer suppressions for TaskSmack tests
#
# SDL3 retains internal video/audio subsystem state after SDL_Quit() that the
# LeakSanitizer reports as leaked.  This is a known false positive: the memory
# is held in SDL3's own shared library and is not a bug in our code.
leak:libSDL3
