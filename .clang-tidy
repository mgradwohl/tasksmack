Checks: >
  bugprone-*,
  clang-analyzer-*,
  cppcoreguidelines-*,
  misc-*,
  modernize-*,
  performance-*,
  portability-*,
  readability-*,
  -bugprone-easily-swappable-parameters,
  -bugprone-exception-escape,
  -misc-include-cleaner,
  -modernize-use-trailing-return-type,
  -readability-identifier-naming,
  -readability-identifier-length,
  -readability-magic-numbers,
  -readability-implicit-bool-conversion,
  -portability-avoid-pragma-once,

WarningsAsErrors: ''
SystemHeaders: false

# Only report warnings for headers directly under src/ or tests/ (not in build/).
HeaderFilterRegex: '^(?!.*(/|^)(build|_deps|vcpkg|llvm|dist|coverage)/).*(/|^)(src|tests)/.*\.(h|hpp|hh|hxx|inl|ipp)$'

FormatStyle: file

# Ignore external libs in include-cleaner
CheckOptions:
  - key: misc-include-cleaner.IgnoreHeaders
    value: >-
      (.*/build/.*|.*/_deps/.*|.*/dist/.*|.*/coverage/.*|.*/spdlog/.*|.*/vcpkg/.*|.*/gtest/.*|.*/gmock/.*)
