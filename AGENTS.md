# AGENTS.md

Guidance for AI agents working in this repo. Build/test/lint/architecture guidance already
lives in [CONTRIBUTING.md](CONTRIBUTING.md) and [.github/copilot-instructions.md](.github/copilot-instructions.md) —
read those instead of duplicating them here.

## GitHub workflow (issues, PRs, reviews, CI)

- `gh` is authenticated against `mgradwohl/tasksmack`; use it directly for issues, PRs, reviews, and workflow runs.
- Follow [CONTRIBUTING.md](CONTRIBUTING.md)'s "Pull Request Process" and "Code Review Expectations" sections: small
  focused PRs, full PR template, `clang-tidy`/`clang-format`/`pre-commit` run before opening, and CI green before
  requesting merge.
- Use the existing templates rather than freehand text: `.github/pull_request_template.md` for PRs,
  `.github/ISSUE_TEMPLATE/bug_report.md` / `.github/ISSUE_TEMPLATE/feature_request.md` for issues.
- Never force-push to a PR branch once a review has started, and never merge, close, or force-push anything without
  asking first — these are shared/hard-to-reverse actions per the standing safety rules. Opening a draft PR, pushing
  a first version, and commenting/reviewing are fine to do directly when asked.
- Prefer `/code-review` before opening a PR, and `/code-review ultra <PR#>` for a deeper multi-agent pass on an
  existing PR.
- CODEOWNERS auto-requests `@mgradwohl` on every PR — no need to manually assign reviewers.
- Never edit `CHANGELOG.md` — it's generated by `git-cliff` from a `v*.*.*` tag push and PR'd automatically.
- Treat `.github/workflows/*` edits as needing explicit confirmation before pushing, even though the `gh` token has `workflow` scope — CI/CD changes get extra human review on this project.

## Quick reference (condensed from CONTRIBUTING.md / copilot-instructions.md)

Architecture: `Platform` (raw OS counters only) → `Domain` (deltas/rates/history/immutable snapshots) →
`Core` (SDL3 window/loop, `PathService`) → `UI` (ImGui/ImPlot, never touches Platform) → `App` (panels; the
only composition root allowed to call `Platform::make*Probe()`/`Platform::makeProcessActions()`, only at
construction/`onAttach`, never in render loops). OpenGL calls confined to `Core`/`UI`.

Build (CMake Presets, `cmake --list-presets` for the full set):
```bash
cmake --workflow --preset dev        # Linux: configure+build+test
cmake --workflow --preset win-debug  # Windows equivalent
```
Common presets have a `win-` twin: `debug`, `release`, `release-compatible`, `optimized`, `relwithdebinfo`,
`coverage`, `unity`, `pgo-generate`/`pgo-use`, `profile`. Linux-only: `asan-ubsan`, `tsan`, `msan`.

Format/lint (required before a PR is ready):
```bash
./tools/clang-format.sh              # or --changed-only for just the git diff
./tools/clang-tidy.sh debug
./tools/check-format.sh              # check without modifying
```
A `PostToolUse` hook (`.claude/settings.json`) already runs `clang-format -i` on any `.cpp`/`.h`/`.hpp`/`.cc`/`.cxx`
file I edit or write, so hand-running the formatter is a backstop, not a requirement, for files I touched.

Test: `ctest --preset debug` / `ctest --preset win-debug` (Google Test; mocks in `tests/Mocks/MockProbes.h`;
use `EXPECT_DOUBLE_EQ`, not `EXPECT_EQ`, on floats).

Naming: `PascalCase` classes, `camelCase` functions, `m_camelCase` members, `UPPER_SNAKE_CASE` constants.
Sampling literals live in `src/Domain/SamplingConfig.h` — reuse them, don't re-declare.

Python tooling (GLAD codegen, IWYU, pre-commit) expects the project venv: `source .venv/bin/activate` before
running `tools/*.sh` scripts that shell out to Python, if a venv exists at `.venv/`.

For a full architecture-boundary/C++23/thread-safety review checklist, see the `tasksmack-review` Claude Code
skill (mirrors `.github/skills/code-review/SKILL.md`, written for Copilot) — run alongside `/code-review`.

## Wayland-Native Support (PR #743, implementing issue #593; validation #382)

All platform-specific window behavior uses `Core::VideoBackend` abstraction:

**Backend Queries:**
- `VideoBackend::isWayland()`, `isX11()`, `isXWaylandFallback()`, `isWindows()`
- `supportsClientSideMaximize()`, `supportsGlobalMouseState()`
- Never directly parse `SDL_GetCurrentVideoDriver()` outside VideoBackend

**Maximize/Restore:** Native Wayland delegates to compositor; X11/XWayland/Windows use client-side positioning.

**Drag Handling:** Wayland uses event-consistent local window coordinates; X11/XWayland/Windows use global mouse state.

**Compositor Safety:** Never assume GNOME/KDE/Sway/Hyprland specifics; implement graceful fallback. Test matrix: `wayland-test-matrix.md`.
